<template></template>

<script setup>
  const router = useRouter();
  // const route = useRoute();

  const items = ref([]);
  const isLoading = ref(false);
  const error = ref(null);

  // const signIn = () => {
  //   router.push('/sign/in');
  // };
  // const signUp = () => {
  //   router.push('/sign/up');
  // };

  onMounted(() => {});

  // definePageMeta({
  //   layout: 'default',
  // });
</script>
